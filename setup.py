from setuptools import setup

requires = [
    'premailer>=3.10.0'
]

setup(
    name='smart_emails',
    version='1.0.0',
    packages=['smart_emails'],
    url='',
    license='',
    author='Vima',
    author_email='',
    description='',
    python_requires=">=3.11",
    install_requires=requires
)
