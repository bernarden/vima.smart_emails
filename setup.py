from setuptools import setup

requires = [
    'premailer>=3.6.0'
]

setup(
    name='smart_emails',
    version='',
    packages=['smart_emails'],
    url='',
    license='',
    author='Megan Ganley',
    author_email='',
    description='',
    python_requires=">=3.6",
    install_requires=requires
)
